---
title: Libraries
icon: "books"
iconType: "solid"
---


Lbraries is the most important part of our Dashboard. In this page you can access the whole content that seperated by modules. In this point you should get help from the given codes because they are built by content and they are easier way to use Upsonic.

When you make a [dump operation](/client/using/dump), its effected instantly to here (just refresh the page). Because they are the same database soure.

<Note>When you click first time to an content its take some times beacuse in the background the documentation is generating. So you should just be patient, its will load after generation.</Note>



## Inside of an module

In this section you can see the sub elements of the top module and in the table you can thee the sub spaces with `Library` text. When you see it you can understand to ability to view inside with click. 

### Gathering Code

This connection code is uses [upsonic.load_module](/client/using/load#upsonic-load-module) function and when you use it you can get all sub contents in just one function as module type.



<img height="300" src="/images/libraries_with_info.svg" />
